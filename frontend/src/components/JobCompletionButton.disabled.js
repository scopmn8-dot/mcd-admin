// Archived: JobCompletionButton removed from the dashboard UI.
// Kept as a disabled stub for history. The active dashboards no longer import this file.

import React from 'react';

export default function JobCompletionButton() {
  if (typeof console !== 'undefined' && console.warn) {
    console.warn('JobCompletionButton (archived) was imported — it is intentionally disabled and returns null.');
  }
  return null;
}
